# protobuf is pinned to 4.23.4 because apache-flink==1.19.1 (see
# stream_processing/flink_image/Dockerfile) transitively depends on
# apache-beam, which caps protobuf<4.24.0. Every generated *_pb2.py stub
# under gen/python must be built with a toolchain targeting this same
# runtime or grpc.aio and PyFlink jobs read incompatible wire formats.
protobuf==4.23.4

# grpcio (the runtime used by grpc.aio) is NOT coupled to protobuf's
# codegen version the way grpcio-tools is — safe to stay current.
grpcio==1.80.0

# grpcio-tools is pinned old specifically because newer releases require
# protobuf>=6.31, which conflicts with the 4.23.4 pin above. Older
# grpcio-tools needs pkg_resources (removed from setuptools>=81), so
# setuptools is pinned alongside it purely as a codegen-time dependency.
grpcio-tools==1.59.0
setuptools<81

aiokafka==0.12.0
pytest==8.4.2
pytest-asyncio==1.2.0

# warehouse/ (Checkpoint 6): lake_writer writes Parquet, clickhouse_loader
# inserts via ClickHouse's HTTP interface.
pyarrow==25.0.1
httpx==0.28.1
